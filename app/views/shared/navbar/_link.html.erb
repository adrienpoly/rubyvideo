<% active = current_page?(path) %>

<%= content_tag :li, class: class_names("pt-1", active: active) do %>
  <%= link_to link_title, path %>
  <%= content_tag :div, "", data: {turn_exit: !active.to_s, turn_enter: active.to_s} %>
<% end %>
