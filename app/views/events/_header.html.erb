<div
  class="w-full md:h-[198px] lg:h-[267px] xl:h-[336px] 2xl:h-[405px] justify-center align-center flex mt-3 border-t border-b"
  style="
    view-transition-name: hero;
    <% if event.banner_background.start_with?("data:") %>
      background: url('<%= event.banner_background %>');
      background-repeat: no-repeat;
      background-size: cover;
    <% else %>
      background: <%= event.banner_background %>;
    <% end %>
  ">
  <%= image_tag image_path(@event.banner_image_path), class: "w-full md:container" %>
</div>

<div class="container py-8">
  <div class="justify-between block lg:flex gap-8 align-center">
    <div class="flex flex-col items-center mb-6 text-center lg:flex-row gap-8 lg:justify-right lg:text-left lg:mb-0">
      <%= image_tag image_path(event.avatar_image_path),
            class: "rounded-full border border-[#D9DFE3] size-24 md:size-36",
            alt: "#{event.name} Avatar",
            style: "view-transition-name: avatar",
            loading: :lazy %>

      <div class="flex flex-col justify-center">
        <h1 class="mb-2 font-bold text-black" style="view-transition-name: title"><%= event.name %></h1>
        <h3 class="text-[#636B74]"><%= event.location %> • <%= event.formatted_dates %></h3>
        <% if event.website.present? %>
          <%= external_link_to event.website.gsub(%r{^https?://}, "").gsub(%r{/$}, ""), event.website %>
        <% end %>
      </div>
    </div>

    <div class="flex flex-col gap-3 place-items-center">
      <%= ui_button "View all #{event.organisation.name} events", url: organisation_path(event.organisation), kind: :secondary, size: :sm, class: "w-full" %>
    </div>
  </div>

  <p class="mt-6 text-[#636B74] max-w-[700px]">
    <%= event.description %>
  </p>
</div>
